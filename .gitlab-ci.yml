test:js:
  image: node:latest
  cache:
    paths:
    - node_modules/
  before_script:
   - npm install standard
  script:
   - ./node_modules/.bin/standard .

test:python:
  image: python:latest
  before_script:
   - pip3 install flake8
  script:
   - flake8 --max-line-length=140 grid/
